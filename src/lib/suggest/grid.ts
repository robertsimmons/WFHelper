/** The card box, in px. The card applies these so every section lays its cards
 *  out on the same grid. */
export const CARD_WIDTH = 288;
export const CARD_HEIGHT = 192;
export const CARD_GAP = 12;
